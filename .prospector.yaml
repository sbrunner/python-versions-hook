inherits:
  - utils:base
  - utils:no-design-checks
  - duplicated
  - utils:fix

mypy:
  options:
    python-version: '3.10'

bandit:
  disable:
    - B607 # Starting a process with a partial executable path
ruff:
  options:
    target-version: py310
